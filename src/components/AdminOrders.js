const AdminOrders = () => {
    // TODO: this screen should allow the admin to select a user maybe from a dropdown or search bar. This should show the orders for that specific user
    // TODO: this screen is where the admin can set delete or edit orders

    // need to make sure the user has admin rights before accessing this page

    return (
        <h2>Admin Orders</h2>
    )
}

export default AdminOrders
